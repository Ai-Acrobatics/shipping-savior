# Shipping Savior — UX/Functional Audit (April 27, 2026)

**Audit by:** cli-session (autonomous agent)
**Method:** Browserbase + Firecrawl screenshots + source code review across homepage, dashboard, login, demo, pricing, savings, tracking
**Live URL:** https://shipping-savior.vercel.app
**Previous audit:** `UX-AUDIT-2026-04-07.md` (some items still open)

---

## Critical (P1) — Blocks investor demo / breaks core flows

| # | Issue | Where | Evidence |
|---|---|---|---|
| C1 | Sidebar logo wordmark "Shipping" is dark-text on near-black sidebar (`bg-[#030d1a]`) — virtually invisible across **every** dashboard page | `src/components/dashboard/DashboardLayout.tsx:46,155` | Logo wrapper `<span class="font-bold text-sm">` has no text color, so "Shipping" inherits default dark navy. Only "Savior" (gradient) shows |
| C2 | Dashboard renders **two stacked headers**: outer chrome (Overview / bell-4 / Live / settings / JS) + inner per-page header (ShippingSavior · Dashboard \| Routes \| FTZ Zones \| Proposal / bell-2 / Last updated / JS) | `src/app/dashboard/page.tsx:395-447` ships its own `<header>` on top of `DashboardLayout` chrome | Two avatars, two notification bells with **different counts** (4 vs 2), two refresh buttons |
| C3 | `/calculators` returns **404** but the top nav links to it on every page | `src/app/page.tsx` header nav | Same bug flagged in 2026-04-07 audit (M2) — still not fixed |
| C4 | Savings page KPI cards render **empty values**: "Total Savings (Q1)", "FTZ Savings", "Avg Savings/Shipment" all show label + subtitle but no $ amount | `src/app/dashboard/savings/page.tsx` | Screenshot confirms missing values; data hook likely undefined |
| C5 | Savings table "Source" column is empty for every row (header rendered, cells blank) | Same page | Likely missing field in seed data |
| C6 | Hero rotating-word animation produces a giant empty gap between "Ship" (line 1) and the rotating word (line 2 — invisible during 280ms blur transition) — looks broken on every keyframe transition | `src/app/page.tsx:308-321` | `<span class="block">Ship</span>` then a separate block span for the word — when `wordVisible=false` the second block goes `opacity-0 blur-md` leaving "Ship" alone with whitespace |
| C7 | "What can we help you ship?" hero heading on homepage is white-on-light-blue (text-white inherited from dark mode context, but rendered on the light gradient hero) — fails AA contrast | `src/components/ui/v0-ai-chat.tsx:84-130` + page section background | Screenshot shows ghosted heading. Heading uses `text-black dark:text-white` — homepage page is force-rendering dark variant |
| C8 | Dashboard demo data is **stale**: Operations cards show shipments with ETAs Mar 28 - Apr 14, 2026 still in "In Transit" — today is Apr 27, 2026 | `src/lib/data/dashboard.ts` (seed) | Multiple ETAs already passed but cards show in-transit progress bars at 67/78/82/90% |
| C9 | "Cost vs. Margin" widget says "March 2026" — current month is April 2026 | Dashboard right panel | Hard-coded month label |
| C10 | Recent Calculations table dates: Mar 20-25, 2026 — older than 1 month with no Apr entries | Bottom of dashboard | Stale seed data |

## High (P2) — Demo polish / professional perception

| # | Issue | Where |
|---|---|---|
| H1 | Login page has **no top navigation** — user can't get back to marketing site without browser back | `src/app/(auth)/login/page.tsx` |
| H2 | Login page has **no "Forgot password?"** link, no SSO option, no demo-account hint | `src/app/(auth)/login/page.tsx` |
| H3 | `/forgot-password` and `/reset-password` routes both 404 | n/a — routes don't exist |
| H4 | Pricing comparison table has **empty cells** where checkmarks should render: Carrier comparison, Reliability scores, PDF exports, API access, Custom integrations, White-label option | `src/app/pricing/page.tsx` | The icon/check component fails for boolean-true rows |
| H5 | Demo page: 4 scenario cards all link to generic `/dashboard` instead of dashboard with the specific scenario pre-loaded (filter / route param) | `src/app/demo/page.tsx` |
| H6 | Demo page progress indicator shows "0/4" but never advances — appears to be a step counter that does nothing | `src/app/demo/page.tsx` |
| H7 | Sidebar Ship icon is `text-navy-900` (dark) on mid-blue gradient — should be `text-white` for the contained icon | `src/components/dashboard/DashboardLayout.tsx:43` |
| H8 | Hero CTAs duplicated: nav "Live Demo" + nav "Demo" + hero "See Live Demo" + hero "Watch How It Works" — same destination | `src/app/page.tsx` header + hero |
| H9 | Homepage "Four intelligence layers" auto-rotator lists FTZ Strategy & Savings **twice** (third-position duplicate) | `src/app/page.tsx` features array |
| H10 | Top nav has 7 items: Platform, Calculators, Carriers, Demo, Pricing, Dashboard, More — exceeds the 5-6 cap from prior audit (M7 still open) | `src/app/page.tsx` Header |
| H11 | "Built by AI Acrobatics" footer label still present despite prior audit flagging it for investor-facing site | `src/app/page.tsx` footer |
| H12 | Savings chart x-axis shows Q1 2023 - Q1 2024 — should be 2025/2026 quarters | `src/app/dashboard/savings/page.tsx` |
| H13 | `/dashboard` route is publicly accessible with no auth wall — contradicts M1 "platform requires auth" plan; investor demo benefits from it being open, but production confusing | `src/middleware.ts` only protects `/platform/*` |

## Medium (P3) — Quality / consistency

| # | Issue | Where |
|---|---|---|
| M1 | Logo treatment inconsistent: homepage uses "ShippingSavior" (no space, blue+grey), login page uses "Shipping Savior" (space, plain) | `src/app/(auth)/login/page.tsx` vs nav |
| M2 | KPI "Container Utilization 78% ↘ -1.8%" — no context whether 78% is good or bad; trend uses red but value isn't tagged as a problem state | `src/app/dashboard/page.tsx` kpis |
| M3 | "Live" pill in dashboard top-right is styled like a button but isn't interactive — unclear UX role (status indicator? toggle?) | `src/components/dashboard/DashboardLayout.tsx:127` |
| M4 | Filter / Export buttons in Operations Dashboard are static — no dropdown / download fires | `src/app/dashboard/page.tsx:469-478` |
| M5 | "Back to Proposal" link at the bottom of `/dashboard` reveals it's a demo, not a product | `src/app/dashboard/page.tsx` |
| M6 | Floating ship-icon chat bubble (bottom-right) appears on auth pages where chat would be unauthenticated | likely global widget mount |
| M7 | 404 page is the Next.js default ("404 / This page could not be found / Ask AI") with no helpful link home or search | `src/app/not-found.tsx` (missing) |
| M8 | Demo cards show `0/4` step counter that never updates and has no caption explaining its meaning | `src/app/demo/page.tsx` |
| M9 | Tracking shipment cards show ETAs older than today with status "In Transit" — same staleness as dashboard | `src/lib/data/dashboard.ts` |
| M10 | Sidebar "Notifications" badge shows 4, but inner-page bell shows 2 on dashboard (not Tracking/Savings, where they match at 4) — inconsistent badge sources | `DashboardLayout.tsx` vs `dashboard/page.tsx` per-page header |

## Low (P4) — Polish / nice-to-have

| # | Issue | Where |
|---|---|---|
| L1 | "Trusted by..." marquee duplicates each carrier (Chiquita, Kingsco, Hall Pass…) intentionally for infinite scroll, but pre-render shows the duplicate list as a static list when JS hasn't hydrated | `src/app/page.tsx` |
| L2 | Pricing FAQ questions render but answers are not visible without JS expand — accessible if JS, but no fallback `<details>` markup | `src/app/pricing/page.tsx` |
| L3 | Sitemap only has 10 entries — missing /agreement, /jv-agreement, /knowledge-base, /tech-spec, /six-sigma, /platform-architecture | `src/app/sitemap.ts` |
| L4 | OG image is SVG (`og-image.svg`) — many platforms (LinkedIn, Slack) prefer PNG/JPG; SVG often rejected | `src/app/layout.tsx` metadata |

---

## Recommended Demo-Critical Fix Order (Before May 11 Investor Pitch)

1. **C1** sidebar logo (5-min CSS fix: add `text-white` to wrapper span) — fixes every dashboard page
2. **C2** remove duplicate header in `dashboard/page.tsx` (delete lines 397-448; rely on layout chrome)
3. **C8** + **C9** + **C10** refresh demo data to current month (April 2026)
4. **C4** + **C5** wire savings page KPI cards + populate Source column
5. **C6** fix hero animation: drop `block` from one span; render rotating word inline so "Ship X" reads as one phrase
6. **C7** fix dashboard chat heading contrast (force `text-navy-900` regardless of dark mode parent)
7. **C3** create `/calculators` route or update nav to point to existing `/platform/calculators` (requires login — alternative: the public-facing `/dashboard`)
8. **H4** fix pricing comparison table check-marks
9. **H1** + **H2** + **H3** add forgot-password page and login footer link to home
10. **H10** + **H11** trim nav to 5 items and remove "Built by AI Acrobatics" footer
