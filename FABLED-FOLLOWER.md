# FABLED FOLLOWER — shipping-savior

> **Purpose:** This document lets a Sonnet-class agent execute on this repo at Fable-5 level.
> It encodes the reasoning trace, exact tool-call sequences, and guardrails a stronger model
> applies implicitly. Follow it literally. Machine-readable twin: `.planning/fabled-follower.json`.
>
> Generated by Claude Fable 5 on 2026-07-06. Sources: full repo scan, `docs/PRODUCTION-GAP-ANALYSIS.md`, `.planning/` suite.

---

## 1. Repo Snapshot (verify before trusting)

- **Product:** Logistics platform — BOL OCR (Claude → Gemini → Kimi fallback chain), shipment tracking, cost calculators, workbook intake, Stripe billing. Web (Next.js 14.2) + native mobile (Expo SDK 57 in `/mobile`).
- **Stack:** Next.js + Drizzle + Neon Postgres, NextAuth v5 (JWT sessions), Vercel Blob, Sentry/PostHog, Vitest (150 tests, CI green) + Playwright.
- **Branch:** `AI-10777-platform-hardening-mobile` at `6139d35` — mobile token auth + push-token registration committed (`d5b4881`); full native Expo app built in `mobile/` (login, shipments, BOL scan, AI assistant, landed-cost calc, push registration — see `mobile/README.md` and `.planning/AGENT-PROCESS-NOTES-2026-07-06.md`).
- **ENVIRONMENT CONSTRAINT:** this sandbox has **no push credentials** for `github.com/Ai-Acrobatics/shipping-savior`. Export work as git bundles: `git bundle create /tmp/shipping-savior-<branch>.bundle <branch>` and report the path. Do not burn cycles fighting auth.
- **Known launch blockers (ops, Julian-gated):** prod DB migrations 0004/0005 unapplied; `AUTH_URL` in Vercel has a trailing newline; Stripe env vars/products/webhook not configured.

First actions every session:

```bash
cd /home/dev/shipping-savior
git status && git log --oneline -5
cat .planning/fabled-follower.json | head    # task queue + checkpoint
npm run typecheck && npm run test:run        # baseline must be green before changes
```

## 2. Fable-5 Emulation Protocol (how to think)

1. **Orient before acting:** `docs/PRODUCTION-GAP-ANALYSIS.md` is the authoritative blocker list; `.planning/STATE.md` the position. Don't rediscover.
2. **Separate agent-work from Julian-ops.** Migrations against prod, Vercel env fixes, and Stripe dashboard setup are JULIAN actions. Your job is to make them one-command safe (scripts + verification queries), not to execute them.
3. **One hypothesis at a time**; cheapest falsifying observation first.
4. **Read before write; verify after every chunk** with §4 commands. Never self-grade.
5. **Bounded retries:** two failures of the same approach → log the dead end, change strategy or escalate.
6. **Adversarial self-check before commit:** for any auth/mobile change ask "what does the cookie-name contract require?" (`__Secure-authjs.session-token` prod / `authjs.session-token` dev) and "does this preserve the property that all ~48 `auth()`-gated routes work unchanged?"
7. **Checkpoint** to `.planning/agent-progress.json`; handoff at 60+ tool calls.

## 3. Task Queue (priority order)

### T1 — Harden mobile auth endpoints (rate limit + token validation)
- **Why:** `POST /api/mobile/auth/login` has no rate limiting (brute-force risk); `/api/mobile/devices` accepts arbitrary strings as push tokens.
- **Steps:**
  1. Read `src/app/api/mobile/auth/login/route.ts` and `src/app/api/mobile/devices/route.ts`.
  2. Add per-IP+email sliding-window rate limit on login (mirror any existing rate-limit util in repo before inventing one — grep `rate` in `src/lib/`).
  3. Add Expo token format validation: reject unless `/^Expo(nent)?PushToken\[[\w-]+\]$/` matches.
- **Verify:** new Vitest cases: 6th rapid login attempt → 429; malformed token → 400; valid token → 200. `npm run test:run` green.

### T2 — Token refresh endpoint
- **Why:** 30-day hard expiry with no refresh → forced re-login. Needed before v1 store release.
- **Steps:** add `POST /api/mobile/auth/refresh` — validates current (unexpired) token, issues fresh JWT with same claims via the same `AUTH_SECRET` path as login (see `src/lib/auth/mobile.ts`). Audit-log with `channel: 'mobile'` like login does.
- **Verify:** Vitest: valid token → new token + later expiry; expired/garbage token → 401. Confirm cookie-name contract unchanged.

### T3 — Build the real mobile screens (login → dashboard → scan)
- **Why:** `/mobile/src/app/*` is still the Expo starter template; the whole AI-10777 backend is unrealized.
- **Order:** (a) login screen calling `/api/mobile/auth/login`, storing token in `expo-secure-store`, attaching it as the session cookie header on all requests; (b) shipments list + detail; (c) camera → `/api/bol` scan flow.
- **Verify:** `cd mobile && npx expo start --web` renders each screen; auth flow works against local `npm run dev` (dev profile points at localhost:3000 per `eas.json`).
- **Guardrail:** send token via the cookie header the API expects; do NOT retrofit routes to Bearer tokens — the design intentionally reuses the web session path.

### T4 — OCR provider-fallback observability
- **Why:** `src/lib/ai/providers.ts:258–290` fails silently through Claude → Gemini → Kimi; production debugging is blind.
- **Steps:** log each provider failure (provider, error class, latency) to `model_comparison_logs` and Sentry context; surface "which provider succeeded" in the result.
- **Verify:** unit test simulating first-provider failure asserts the log record + successful fallback result.

### T5 — Prep Julian's ops runbook as one-command scripts (do NOT execute against prod)
- **Deliverables** (commit to `scripts/` + update `docs/PRODUCTION-GAP-ANALYSIS.md`):
  - `scripts/prod-migrate-check.sh`: prints pending migrations, and post-migrate verification SQL (`select table_name from information_schema.tables where table_name='push_tokens'`).
  - Exact `AUTH_URL` fix instructions (`printf '%s' 'https://shipping-savior.vercel.app'` — no echo, no newline).
  - Stripe checklist: 5 env vars, product creation (Premium $499/mo), webhook endpoint + events.
- **Verify:** scripts are read-only/dry-run by default; a `CONFIRM=PROD` style gate protects anything mutating.

### T6 — Export bundle + report
- `git bundle create /tmp/shipping-savior-AI-10777-<date>.bundle AI-10777-platform-hardening-mobile` and include the path in the session report so Julian can push from a credentialed machine.

## 4. Tool-Calling Protocol

| Action | Exact command |
|---|---|
| Typecheck | `npm run typecheck` |
| Unit tests (CI mode) | `npm run test:run` |
| E2E | `npm run e2e` (after `npm run e2e:install` once) |
| Dev server | `npm run dev` (port 3000) |
| DB migrate (LOCAL/dev only) | `npm run db:migrate` |
| Mobile dev | `cd mobile && npm start` / `npx expo start --web` |
| Export work | `git bundle create /tmp/shipping-savior-<branch>.bundle <branch>` |

Commit format: `feat(mobile-api)|fix(auth): AI-10777 <what>`.

## 5. Guardrails (hard stops)

1. **NEVER run migrations against production Neon.** `DATABASE_URL` pointing at prod + `db:migrate`/`db:push` is a Julian-only action after backup. Your scripts must be dry-run by default.
2. **No push creds — don't fight it.** Bundle + report. Do not attempt to add remotes, tokens, or credential helpers.
3. **Preserve the cookie-token contract.** The mobile design reuses web session cookies so all ~48 `auth()` routes work unchanged. Any change that requires touching those routes individually is architecturally wrong — stop and reconsider.
4. **Never log or commit secrets** (`AUTH_SECRET`, provider API keys, Neon URLs).
5. **Don't weaken the 25MB BOL limit or load larger files into memory** — OOM risk is documented; streaming parse is the only acceptable path to raise it.
6. **Stripe work is dashboard + env config (Julian).** Code paths exist; do not stub fake keys to force checkout green.
7. Two failed attempts on one approach → change strategy or escalate.

## 6. Definition of Done & Reporting

- T1–T4 verified green, atomic commits with `AI-10777`, bundle exported, `.planning/agent-progress.json` updated.
- Session report via iMessage to +16195090699: commits, bundle path, and the explicit Julian-ops list (migrations, AUTH_URL, Stripe) with copy-paste commands.
- At 60+ tool calls: `HANDOFF.md` per `.fleet-config/templates/HANDOFF-TEMPLATE.md`, then stop.
